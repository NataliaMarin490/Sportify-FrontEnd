<form
            className="form-account" //</div>onSubmit={handleSubmit}
          >
            <span className="titulo-account">Crea tu cuenta</span>
            <div className="input-container">
              <label className="label">Nombres</label>
              <input
                className="entrada-registrer"
                type="text"
                name="name"
                placeholder="ej. Juan Miguel"
                // value={formData.name}
                //onChange={handleChange}
                required
              />
            </div>
            <div className="input-container">
              <label className="label">Apellidos</label>
              <input
                className="entrada-registrer"
                type="text"
                name="name"
                placeholder="ej. Pérez Rodríguez"
                // value={formData.name}
                //onChange={handleChange}
                required
              />
            </div>
            <div className="input-container">
              <label className="label">Número celular</label>
              <input
                className="entrada-registrer"
                type="email"
                name="email"
                placeholder="ej. +XXX XXXX XXXX"
                //value={formData.email}
                //onChange={handleChange}
                required
              />
            </div>
            <div className="input-container">
              <label className="label">Correo electrónico</label>
              <input
                className="entrada-registrer"
                type="email"
                name="email"
                placeholder="ej. ejemplo@gmail.com"
                //value={formData.email}
                //onChange={handleChange}
                required
              />
            </div>
            <div className="input-container">
              <label className="label">Fecha Nacimiento</label>
              <input
                className="entrada-registrer"
                type="date"
                name="birthdate"
                placeholder="ej. 01/01/2000"
                //value={formData.password}
                //onChange={handleChange}
                required
              />
            </div>
            <div className="input-container">
              <label className="label">Ciudad</label>
              <input
                className="entrada-registrer"
                type="text"
                name="city"
                placeholder="ej. Medellín"
                //value={formData.password}
                //onChange={handleChange}
                required
              />
            </div>
            <div className="input-container">
              <label className="label">Contraseña</label>
              <input
                className="entrada-registrer"
                type="password"
                name="password"
                placeholder="***********"
                //value={formData.password}
                //onChange={handleChange}
                required
              />
            </div>
            <div className="input-container">
              <label className="label">Confirma Contraseña</label>
              <input
                className="entrada-registrer"
                type="password"
                name="password"
                placeholder="***********"
                //value={formData.password}
                //onChange={handleChange}
                required
              />
            </div>
            <button className="button-account">Registrarse</button>
          </form>